module.exports = {
    server: {
      // Comando del servidor
      command: "yarn serve",
      // Puerto de escucha
      port: 8080,
      // Tiempo de espera para el lanzamiento
      launchTimeout: 40000,
      // Mostramos los mensajes del comando
      debug: true,
    },
  }
  